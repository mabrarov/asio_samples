{{- if .Values.image.pull.secret -}}
apiVersion: "v1"
kind: "Secret"
metadata:
  name: {{ include "tcp-echo.imagePullSecretName" . | quote }}
  labels:
    {{- include "tcp-echo.labels" . | nindent 4 }}
    {{- include "tcp-echo.componentLabels" . | nindent 4 }}
type: "kubernetes.io/dockerconfigjson"
data:
  .dockerconfigjson: {{ include "tcp-echo.dockerRegistryAuthenticationConfig" (dict "imageRegistry" .Values.image.registry "credentials" .Values.image.pull.secret) | quote }}
{{- end }}
